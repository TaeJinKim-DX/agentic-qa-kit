# STATE.md — QA 상태 (재개 계약)

> 2부 구조: **① 상태 스냅샷**(항상 최신값으로 덮어쓴다) + **② 결정 로그**(append-only —
> 기존 행 수정·삭제 금지, 아래에 행 추가만). 세션 시작 시 이 파일부터 읽는다(AGENT.md §2).
> 지금 세션이 죽는다고 가정하고, 다음 에이전트가 이 파일만 보고 정확히 이어받을 수 있게
> 발생 즉시 갱신하라(원칙 9).

---

## ① 상태 스냅샷

### 버전

| 항목 | 값 |
|---|---|
| kit_version | `<킷 CHANGELOG.md 버전>` |
| config_schema_version | `<qa-config.yaml meta 값>` |

<!-- 킷 최신과 불일치 시: 전체 재인터뷰 금지 — 바뀐 질문만 diff 인터뷰(AGENT.md §2) -->

### 현재 페이즈 + 게이트 이력

| 항목 | 값 |
|---|---|
| 현재 페이즈 | `<P0~P5>` |
| 게이트1 — 설정 승인 | `<YYYY-MM-DD HH:MM 통과 / 미통과>` |
| 게이트2 — 케이스 승인 | `<YYYY-MM-DD HH:MM 통과 / 미통과>` |
| 게이트3 — 결과·known-issue 승인 | `<YYYY-MM-DD HH:MM 통과 / 미통과>` |
| 게이트4 — 스케줄 허가 | `<YYYY-MM-DD HH:MM 통과 / 미통과>` |

### 진행 중 run + 케이스 커서

| 항목 | 값 |
|---|---|
| run-id | `<run-id> (runs/<env>-<run-id>/) / 없음` |
| 완료 | `<A-01, A-02, …>` |
| 실행중 | `<B-01 / 없음>` |
| 미착수 | `<B-02, D-01, …>` |

<!-- 케이스 ID = <카테고리>-<번호> (A-01 식). 증거는 runs/<env>-<run-id>/evidence/<case-id>/.
     실행 상호배제는 qa/.lock (stale 판정 30분). 진행 중 run이 남아 있으면
     다음 세션은 아래 잔여물 대장 정리·검증부터 시작한다(AGENT.md §2). -->

### 시딩 잔여물 대장

<!-- 리소스 생성 즉시 행 추가, 정리 검증(잔여 0건 확인) 후 "완료"로 갱신(원칙 5).
     세션이 죽어도 다음 세션이 이 표만 보고 치울 수 있어야 한다. -->

| 리소스 식별자 | 유형·위치 | 생성 run-id | 정리 여부 |
|---|---|---|---|
| `<qa-…>` | `<DB 테이블 / 외부 시스템>` | `<run-id>` | `<미정리 / 완료(검증 YYYY-MM-DD HH:MM)>` |

### 능력 프로파일 (P0 탐지)

| 능력 | 값 |
|---|---|
| 브라우저 자동화 | `<대화형 브라우저 도구 / Playwright / 없음(강등 — 브라우저 케이스 수동 표기)>` |
| 구조화 질문 UI | `<있음 / 없음(채팅 번호 질문)>` |
| 헤드리스 재호출 수단 | `<있음(호출 커맨드) / 없음>` |
| 스케줄 수단 | `<cron / OS 작업 스케줄러 / 에이전트 자체 / 없음(반무인 강등)>` |
| Node.js | `<버전 / 없음>` |

### 준비물(clearance) 확보 현황 — 캐시

<!-- 정본은 qa/clearance.md — 이 행은 요약 캐시일 뿐이다. P3 진입 시 반드시 정본을 재확인하라. -->

| 확보 | 갱신 타임스탬프 |
|---|---|
| `<n>/<m>` | `<YYYY-MM-DD HH:MM>` |

### 마지막 디스커버리 기준

<!-- P5 델타 디스커버리의 diff 기준점. 커버리지 신선도 판단에 사용. -->

| 항목 | 값 |
|---|---|
| 커밋 SHA / 앱 버전 | `<…>` |
| 실행일 | `<YYYY-MM-DD>` |

### 다음 액션 (1줄)

`<어떤 에이전트든 이어받아 실행할 첫 문장 — 예: "P2 재개: cases/에 B-03부터 작성">`

---

## ② 결정 로그 (append-only)

<!-- 게이트 승인·주요 결정 발생 즉시 행 추가. 기존 행 수정·삭제 금지. -->

| 일시 | 결정 | 근거·승인 |
|---|---|---|
| `<YYYY-MM-DD HH:MM>` | `<게이트1 승인 / deny-list에 X 추가 / …>` | `<사용자 "승인" / 사용자 "수정: …" 반영 / …>` |
